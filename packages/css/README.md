# @vibe-architecture/css

`@vibe-architecture/css`는 clone한 seed-design `packages/css`에서 필요한 구조만 남긴 최소 subset이다.

반영한 축:
- `vars`
- `vars/component`
- `theming`
- `recipes`
- `base.css`, `all.css`, `index.css`

현재는 학습용으로 `field`, `dialog` 중심 recipe와 token bridge만 유지한다.
